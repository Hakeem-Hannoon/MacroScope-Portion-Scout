{
  "name": "physics-powered-portion-estimation",
  "version": "0.1.0",
  "private": true,
  "description": "(photo, measured scale) → calories + macros + micros. Metric portion estimation from AR geometry, on-device.",
  "license": "MIT",
  "engines": {
    "node": ">=22.5"
  },
  "workspaces": [
    "packages/*",
    "nutrition"
  ],
  "scripts": {
    "typecheck": "npm run typecheck --workspaces --if-present",
    "test": "vitest run",
    "test:watch": "vitest",
    "etl:bundle": "npm run build -w @ppe/nutrition-etl --"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vitest": "^3.0.0"
  }
}
